spring:
  application:
    name: eureka-discovery-service

server:
  port: 8095
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8095/eureka
